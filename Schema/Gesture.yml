title: Gesture
type: object
description: Gesture represents a configuration of the gesture, which is a pair of a pattern and action, plus some additional information
properties:
  id:
    type: string
    format: uuid
  pattern:
    $ref: ./Pattern.yml
  action:
    $ref: ./Action.yml
  enabled:
    type: boolean
required:
  - id
  - pattern
  - action
  - enabled
